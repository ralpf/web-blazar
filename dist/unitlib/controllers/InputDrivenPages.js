import { Unit } from "../Unit.js";
/**
 * A container for content, to show one children at a time
 */
export class InputDrivenPages extends Unit {
    constructor(root) {
        super(root);
    }
}
